const raio = 5.6;
const area = Math.PI * Math.pow(raio, 2); // -> "Math.pow" faz o numero virar potencia por exemplo nesse caso o raio elevado a 2

console.log((area).toFixed(2));
console.log(typeof Math);